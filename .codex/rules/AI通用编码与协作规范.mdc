---
description: 全仓通用的 AI 编码、协作与验证规范
alwaysApply: true
---

# AI 通用编码与协作规范

这份规则适用于当前仓库中的所有项目、模块、脚本、文档与改造任务。

所有 LLM / AI 编程工具在开始分析、编写、修改、重构、调试、补文档、补测试之前，都必须自动读取并遵守本文件。

---

## 1. 先思考，再编码

核心原则：不要假设，不要隐藏困惑，要暴露权衡。

- 实现前必须先说明本次任务的目标、范围、依赖和关键假设
- 如果需求存在多种解释，必须明确列出，不能默认选择其中一种后直接编码
- 如果现有计划、任务、代码现状三者之间存在冲突，必须先指出冲突点，再决定如何处理
- 如果有更简单、更稳妥、更少改动的实现方式，应优先采用，并明确说明原因
- 如果信息不足以安全落地，必须先补充上下文，不能靠猜测编码

## 2. 简单至上

核心原则：只写解决当前问题所需的最少代码，不做推测性设计。

- 不添加需求之外的功能
- 不为一次性使用的代码提前创建抽象
- 不为了“看起来更通用”而引入额外层级、额外配置、额外状态
- 不为当前不会发生的场景编写复杂兜底逻辑
- 如果 50 行能解决，就不要写成 200 行
- 写完后要反问自己：这段实现是否已经超出资深工程师会接受的复杂度；如果是，就继续简化

## 3. 手术式修改

核心原则：只改必须改的地方，只清理因本次修改引入的问题。

- 不顺手重构无关模块
- 不顺手修改相邻但无关的逻辑、注释、命名、格式
- 优先保持现有项目风格一致，先遵循仓库已有写法
- 因本次改动产生的未使用导入、变量、函数，必须一并清理
- 对于历史遗留死代码、旧逻辑、潜在问题，可以记录说明，但除非任务明确要求，否则不要擅自删除
- 每一行改动都应该能追溯到用户需求、任务说明或明确的规范要求

## 4. 目标驱动执行

核心原则：先定义成功标准，再循环实现和验证，直到通过。

- 每次开发都必须先明确：
  - 本次要完成什么
  - 本次不做什么
  - 如何验证已经完成
- 多步骤任务必须拆成清晰的小步骤，避免大段一次性修改
- 每完成一段关键改动，都要有对应的验证动作，例如：
  - 静态检查
  - 类型检查
  - 构建检查
  - 手工验证
  - 日志验证
- 不能只说“理论上没问题”，必须给出实际验证结果，或明确说明未验证原因

## 5. 中文注释与日志要求

这是仓库级强制要求，后续开发必须严格执行。

- 新增或重写的核心逻辑，必须补充清晰的中文注释
- 注释重点说明：
  - 这段代码在做什么
  - 为什么这样做
  - 输入输出或状态变化是什么
  - 这段逻辑与任务目标的关系是什么
- 复杂分支、状态切换、路由回跳、请求拦截、兼容逻辑、边界判断等位置，必须优先加中文注释
- 关键链路必须保留足够的日志打印，方便开发者、LLM 和后续维护者联调与排障
- 日志重点覆盖：
  - 进入关键流程
  - 参数与关键状态变化
  - 分支判断命中情况
  - 异常或兜底处理
  - 关键输出结果
- 日志内容要可读、可搜索、可定位，禁止只打印无语义的短字符

### 5.1 前后端字段中文注释强制要求

这是字段级说明的强制规范，适用于后端模型、后端 schema、前端类型、前端属性、前端状态和接口字段。

- 后端新增或修改数据库模型时，模型中的字段必须补充中文注释
- 后端新增或修改 schema、DTO、请求体、响应体时，字段必须补充中文注释，明确字段业务含义
- 前端新增或修改 `type`、`interface`、`props`、`ref` 状态、`computed` 输出、页面表单字段、接口入参和接口返回字段时，必须补充中文注释
- 前端页面中的关键属性、展示字段、状态字段、流程字段，不能只写字段名，必须说明“这个字段表示什么、什么时候使用、给谁看、和哪段流程有关”
- 如果字段属于兼容字段、过渡字段、缓存字段、派生字段，也必须明确写出“为什么存在”和“后续由谁消费”
- 对于含义不直观、缩写命名、跨端复用、跨模块共享的字段，必须优先补充中文解释，不能省略
- 不允许只写“字段定义”“用户信息”“状态值”这类无业务含量注释，必须解释字段语义
- 本规则同时适用于：
  - 后端 `models`、`schemas`、`services` 中构造的数据结构
  - 前端 `types`、`hooks`、`store`、页面组件中的属性和状态定义
  - 文档中的接口字段说明和计划文档中的字段约束

## 6. 通用性、功用性、封装性要求

- 通用性：优先沉淀可复用的最小公共能力，不把某个项目的特殊业务流程固化为全局实现
- 功用性：代码必须直接服务当前目标，确保新接手的人看得懂、接得上、改得动
- 封装性：公共逻辑应集中在明确边界内，避免调用方散落重复判断和重复协议处理
- 单一职责优先：一个模块只做一类事，一个方法只处理一个清晰目标
- 对外接口尽量稳定：重构内部实现时，优先减少对调用方的破坏
- 能复用公共层时，不要把同样逻辑复制到多个页面、组件或服务中

## 7. 验证与交付要求

- 修改文档时，至少检查链接、路径、引用关系是否正确
- 修改代码时，优先执行与本次改动直接相关的最小验证
- 如果仓库存在历史问题导致无法全量验证，必须明确写出：
  - 卡在哪个文件或命令
  - 这是历史问题还是本次引入的问题
  - 本次已完成了哪些替代验证
- 输出结果时，要让后续开发者快速知道：
  - 改了什么
  - 为什么这样改
  - 还有什么风险或待办

## 8. 禁止事项

- 禁止无依据猜测需求
- 禁止跳过计划、任务或上下文直接编码
- 禁止为炫技而过度封装
- 禁止顺手大面积重构无关代码
- 禁止删除不属于本次任务范围的历史代码
- 禁止省略关键中文注释与关键日志
- 禁止在未说明验证情况的前提下宣称“已经完成”

## 9. 推荐自检清单

- 我是否已经理解本次任务目标、范围、非目标和验收标准
- 我的改动是否严格落在当前任务范围内
- 是否存在可以进一步简化的实现
- 是否补了足够的中文注释
- 是否补了足够的日志打印
- 是否清理了因本次修改产生的孤立代码
- 是否完成了最小必要验证
- 是否把未验证项、历史问题、残留风险写清楚
